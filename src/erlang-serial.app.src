%% ex: ft=erlang
{application, 'erlang-serial', [
	{description, "Erlang driver for serial communication"},
	{vsn, "1.1"},
	{registered, []},
	{applications, [kernel, stdlib]},
	{modules, [serial]}
]}.
